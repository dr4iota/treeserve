[package]
name = "dirview"
version = "0.1.0"
edition = "2024"

[dependencies]
comrak = { version = "0.52.0", default-features = false }
syntect = "5.3.0"
tiny_http = "0.12.0"
two-face = "0.5.1"

[profile.release]
lto = true
strip = true
